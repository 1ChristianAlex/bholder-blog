import { Reducer } from 'redux';

type user = {};
type payload = user;

const userReducer: Reducer<user, payload> = (state, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
};
